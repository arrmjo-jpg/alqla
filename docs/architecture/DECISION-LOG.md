# سجلّ القرارات (Decision Log)

سجلّ مركزيّ لكل قرار تقنيّ/منتجيّ يعتمد عليه أحد بنود
[IMPLEMENTATION-ROADMAP.md](../roadmap/IMPLEMENTATION-ROADMAP.md)، بحيث
لا يضيع سبب اختيار مسار (أ) بدل (ب) بين الاجتماعات. **هذا السجلّ لتوثيق
القرارات الفعليّة بعد اتّخاذها** — البنود أدناه بحالة `معلَّق (Pending)`
تعني أنّ القرار **لم يُتَّخَذ بعد**؛ لا تُقرَأ كقرار نهائيّ.

كل قرار مُتّخَذ يُضاف بصفّ جديد بنفس الجدول عند اتّخاذه فعليّاً، مع تعبئة
عمود "التاريخ" و"القرار" و"السبب" حينها — لا قبل ذلك.

---

## DEC-001 — حقل `is_featured` على مورد قائمة المقالات

| الحقل | القيمة |
|---|---|
| **ID** | DEC-001 |
| **التاريخ** | معلَّق — لم يُتَّخَذ القرار بعد |
| **الحالة** | 🟡 معلَّق (Pending) |
| **القرار** | _(يُملَأ عند الاتّخاذ)_ |
| **البدائل المطروحة** | (أ) إضافة `is_featured` إلى `PublicArticleListItemResource.php` — يُصلِح الجذر لأي مستهلك مستقبليّ أيضاً. (ب) حذف منطق `.find(is_featured)` الميت في `en/category/[id]/[name]/page.tsx` والاعتماد على `items[0]` مباشرة (وهو ما يحدث فعليّاً على أي حال). |
| **السبب** | _(يُملَأ عند الاتّخاذ — يجب أن يوضِّح لماذا أ أو ب، لا فقط ماذا)_ |
| **المسؤول** | Backend (إن أ) / Frontend (إن ب) — القرار نفسه: Engineering Lead |
| **المرتبط بـ Sprint** | Sprint 2 (بند 2.7 في IMPLEMENTATION-ROADMAP.md) |
| **ADR مرتبط** | لا يوجد — قرار تنفيذيّ صغير النطاق، لا يستدعي ADR رسميّاً |

---

## DEC-002 — مصير `/articles/trending` و`/articles/breaking`

| الحقل | القيمة |
|---|---|
| **ID** | DEC-002 |
| **التاريخ** | معلَّق |
| **الحالة** | 🟡 معلَّق (Pending) |
| **القرار** | _(يُملَأ عند الاتّخاذ)_ |
| **البدائل المطروحة** | (أ) الإبقاء عليهما كما هما (احتمال استخدام مستقبليّ). (ب) توثيقهما صراحة كـ"مخطَّط لحذفهما" بلا حذف فعليّ الآن. (ج) حذف فعليّ الآن (كود ميت مؤكَّد من منظور الواجهة الحاليّة). |
| **السبب** | _(يُملَأ عند الاتّخاذ)_ |
| **المسؤول** | Backend + Product |
| **المرتبط بـ Sprint** | Sprint 3 (بند 3.6) |
| **ADR مرتبط** | لا يوجد — قرار تشغيليّ، لا معماريّ |

---

## DEC-003 — معالجة معرّفات الأقسام المُثبَّتة بالكود (Multi-Site/White-Label)

| الحقل | القيمة |
|---|---|
| **ID** | DEC-003 |
| **التاريخ** | معلَّق |
| **الحالة** | 🟡 معلَّق (Pending) — **مشروط بقرار عمل أوسع أوّلاً**: هل توجد خطّة فعليّة لـ Multi-Site/White-Label؟ لا قيمة لمناقشة "كيف" قبل "هل" |
| **القرار** | _(يُملَأ عند الاتّخاذ — يبدأ بـ: هل نمضي في Multi-Site/White-Label أصلاً؟)_ |
| **البدائل المطروحة** | (أ) عدم المضيّ — الإبقاء على المعرّفات المُثبَّتة بالكود طالما لا خطّة فعليّة (الوضع الحاليّ). (ب) المضيّ — نقل معرّفات الأقسام (2/31/36/37/42/43/49/51/53/55/56/58/60/61) من الكود المصدريّ إلى إعداد/CMS. |
| **السبب** | _(يُملَأ عند الاتّخاذ)_ |
| **المسؤول** | Product/Engineering Leadership (قرار استراتيجيّ، لا هندسيّ بحتاً) |
| **المرتبط بـ Sprint** | Sprint 3 (بند 3.8) |
| **ADR مرتبط** | **مرشَّح لِـ ADR جديد (E9) إن اختير المسار ب** — يتبع تسمية سلسلة الامتدادات القائمة في `docs/adr/` (E1، E2، E5–E8؛ E3/E4 غير مُستخدَمين حاليّاً، لا تُعاد تسميتهما). **لا يُنشأ الملفّ الآن** — فقط بعد اتّخاذ القرار فعليّاً. |

---

## DEC-004 — نطاق ومسار توحيد المكوّنات (بطاقات/هيرو/ترقيم)

| الحقل | القيمة |
|---|---|
| **ID** | DEC-004 |
| **التاريخ** | معلَّق |
| **الحالة** | 🟡 معلَّق (Pending) — **يحتاج مدخلاً من Product/Design قبل أي بدء هندسيّ**، كما هو موثَّق في بند 3.1 من الخارطة |
| **القرار** | _(يُملَأ عند الاتّخاذ — أيّ الأنماط "يفوز" لكل سياق: البطاقة الأفقيّة أم العموديّة أم المصغَّرة؟)_ |
| **البدائل المطروحة** | (أ) توحيد جزئيّ تدريجيّ (الخطوة الأولى المقترَحة في 3.1: دمج `HorizontalArticleCard`+`FeaturedCategoryCard` فقط). (ب) مشروع توحيد كامل (7 بطاقات → 2-3، 12 نمط هيرو → أقلّ، 4 أنظمة ترقيم → أقلّ) كمبادرة مستقلّة بخطّة زمنيّة خاصّة بها. (ج) عدم التوحيد — قبول التكرار الحاليّ كأمر واقع موثَّق. |
| **السبب** | _(يُملَأ عند الاتّخاذ)_ |
| **المسؤول** | Product + Design + Frontend Lead |
| **المرتبط بـ Sprint** | Sprint 3 (بند 3.1) |
| **ADR مرتبط** | **مرشَّح لِـ ADR جديد (E10) إن اختير المسار ب** (مشروع كامل) — قرار معماريّ حقيقيّ يستحقّ توثيقاً دائماً بمعزل عن نتيجته. المسار (أ) لا يستدعي ADR (تغيير تكتيكيّ محدود النطاق). **لا يُنشأ الملفّ الآن.** |

---

## كيفيّة استخدام هذا السجلّ

- عند اتّخاذ أي قرار من الأربعة أعلاه فعليّاً: امْلأ **التاريخ**، **القرار**،
  **السبب** في نفس الصفّ — لا تُنشئ صفّاً جديداً لنفس الـ ID.
- عند ظهور قرار جديد يعتمد عليه بند مستقبليّ في الخارطة: أضِف صفّاً جديداً
  بـ ID تالٍ (`DEC-005`...) بنفس البنية.
- لا يُستخدَم هذا السجلّ للقرارات الهندسيّة الروتينيّة (كيفيّة تسمية دالّة،
  مثلاً) — فقط للقرارات التي **لها بديل حقيقيّ كان يمكن اختياره بدلاً منها**
  وتؤثّر على بند تنفيذيّ في الخارطة.
