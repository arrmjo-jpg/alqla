# تقرير Post-Mortem — توقف موقع alqla (القلعة نيوز) بسبب امتلاء القرص

**التاريخ**: 2026-08-07
**الخطورة**: SEV-1 (توقف كامل للموقع والـ API)
**الحالة**: مغلق — الخدمة مستعادة بالكامل، لا فقدان بيانات
**كاتب التقرير**: تحقيق مباشر عبر وصول SSH جذري للسيرفر الإنتاجي (77.237.236.255)، بالتوازي مع مراجعة كود المستودع

---

## 1. Executive Summary

توقف موقع alqla (alpha-cms.shop) بالكامل — الواجهة والـ API ولوحة الإدارة — بعد أن امتلأ قرص السيرفر الإنتاجي 100% (193GB/193GB). السبب الجذري: مجلد كاش Next.js الخاص باستدعاءات بيانات 365Scores (`\.next/cache/fetch-cache`) تضخّم إلى **111GB عبر 3,107,756 ملف**، لأن مسارات صفحات الرياضة (`/sport/match/[id]`, `/sport/player/[id]`, `/sport/team/[id]`, `/sport/competition/[id]`) مفتوحة لأي معرّف رقمي بلا حدود، وكل صفحة مباراة تولّد 6-8 مفاتيح كاش، والكاش الافتراضي لـ Next.js (نظام ملفات، بلا `cacheHandler` مخصّص) لا يحذف أي مفتاح قديم أبداً.

عندما امتلأ القرص، فشلت كل الحاويات (backend/worker/worker-media/scheduler/frontend/admin) بالإقلاع، فدخلت بحلقة إعادة تشغيل متكررة حتى تجاوزت حد Coolify الأقصى (10 محاولات)، فحذف Coolify هذه الحاويات نهائياً — **الموقع توقف بالكامل** لفترة تُقدَّر بعدة ساعات (لا سجلات دقيقة لحظة بدء التوقف الفعلي، راجع القسم 2).

قاعدة البيانات، Redis، وMeilisearch بقوا سليمين وشغّالين طوال الحادثة (غير متأثرين بامتلاء قرص خدمة الكاش، ولم يدخلوا بحلقة إعادة تشغيل). **لم يحدث أي فقدان بيانات**. الاستعادة تمّت عبر: نسخة احتياطية احترازية لقاعدة البيانات، حذف محتوى الكاش المتضخم فقط، ثم إعادة نشر كاملة من Coolify. الموقع مستعادٌ ومستقر بالكامل، تحت إدارة Coolify فقط، بدون أي اعتماد يدوي متبقٍ.

---

## 2. Timeline

جميع الأوقات بالتوقيت المحلي (Asia/Amman، UTC+3) ما لم يُذكر غير ذلك. بعض الأوقات **مُستنتَجة** من أدلة غير مباشرة (uptime الحاويات، تواريخ تعديل الملفات) لا من سجلات وصول مباشرة — لا توجد Access Logs على Traefik (راجع القسم 4)، فلا يمكن تحديد لحظة بدء الانقطاع الفعلي للزوار بدقة.

| التوقيت (تقريبي/مستدل) | الحدث |
|---|---|
| **2026-06-22** | Commit أولي لـ AlphaCMS — كود جلب بيانات 365Scores (`fetch()` بلا حدود IDs) موجود منذ هذه اللحظة |
| **2026-07-25** | نشر يتضمن "sports competitions module" (commit `1c5800ade`) — آخر إعادة تشغيل كاملة موثّقة للـ stack (mysql/redis/meilisearch القديمة بدأت "Up 13 days" عند أول فحص بتاريخ 08-07، ما يعني إقلاعها ~07-25) |
| **2026-07-25 → 2026-08-07** (~13 يوماً) | نافذة تراكم صامتة: `fetch-cache` ينمو بلا أي كشف أو تنبيه — لا مراقبة مساحة قرص، لا تنبيهات 80%/90% |
| **قبل 2026-08-07 (وقت غير محدد)** | القرص يصل 100%، حاويات backend/worker/worker-media/scheduler/frontend فشلت بالإقلاع → Coolify يدخل بحلقة restart متكررة |
| **قبل بداية التحقيق** | Coolify يتجاوز حد الإعادة (65 محاولة مقابل حد 10) ويحذف الحاويات نهائياً — **الموقع متوقف بالكامل**، Meilisearch/Redis/MySQL فقط بقيت شغّالة |
| **~17:08–17:14** | بدء التحقيق: منح وصول SSH جذري، اكتشاف حالة `Exited / 65 restarts / Stopped` عبر لوحة Coolify |
| **~17:14–17:22** | تشخيص أولي: القرص 100% ممتلئ (193G/193G، 436MB متبقٍ)، `docker system df` يكشف `frontend-cache` = 111GB |
| **~17:22–17:26** | فحص أمني عام للسيرفر (UFW، fail2ban، SSH، منافذ مفتوحة) بالتوازي |
| **~17:28** | بدء نسخة احتياطية لقاعدة البيانات (`mysqldump` مضغوط، مباشرة عبر SSH لجهاز محلي — لا مساحة كافية على السيرفر لملف مؤقت) |
| **~17:32–17:37** | حذف محتوى `fetch-cache` فقط (3,107,756 ملف) بالتوازي مع استمرار النسخ الاحتياطي — استغرق 4m59s، حرّر 111GB (القرص أصبح 43% استخدام) |
| **~17:43** | اكتمال النسخة الاحتياطية (482MB مضغوطة، قاعدتا `default` و`gasem`)، التحقق من سلامتها |
| **~17:44–17:47** | تحليل السبب الجذري من الكود مباشرة (مسارات ديناميكية مفتوحة، 8 fetch لكل صفحة مباراة، لا `cacheHandler`) |
| **~17:47 (14:47 UTC)** | محاولة Redeploy أولى من المستخدم — **فشلت**: تبيّن لاحقاً أنها كانت على تطبيق Coolify مختلف (site آخر تمت رؤيته بالخطأ)، وفشل بناء الـ frontend بسبب فحص `build-gate-preflight` (يتطلب backend حيّاً وقت البناء) |
| **~بعد 17:47** | تشخيص مشكلة "chicken-and-egg" بالبناء، محاولة استعادة يدوية لـ backend (استنساخ الكود، بناء صورة، تشغيل حاوية) — تعقيدات شبكية (شبكة `se1ufsuugt5wdb0iw7b40vgv` كانت محذوفة بالكامل من ضمن تنظيف Coolify، أُعيد إنشاؤها) |
| **~18:00–19:50 (تقريباً)** | Coolify ينجح، بشكل مستقل عن المحاولات اليدوية، بإعادة نشر التطبيق كاملاً (على الأغلب بعد تصحيح المستخدم للتطبيق الصحيح على اللوحة) — **لحظة الاستعادة الفعلية غير موثّقة بدقة** لأنها لم تُلاحَظ لحظياً |
| **~19:50–20:00** | اكتشاف أن كل الحاويات (backend/frontend/worker/worker-media/scheduler/admin/meilisearch) شغّالة وصحية منذ ~ساعتين تحت إدارة Coolify — إزالة محاولة يدوية متضاربة (تسببت بـ503 مؤقت بسبب تعارض Traefik router) |
| **~20:00–20:10** | تحقق نهائي: `api.alpha-cms.shop` و`alpha-cms.shop` يردّان HTTP 200، المحتوى صحيح (`<title>القلعة نيوز</title>`) |
| **~20:10–20:20** | تنظيف مخلفات المحاولة اليدوية (صورة `alphacms-backend:latest`، مجلد `/root/alqla-src`) بعد التحقق أنها غير مستخدَمة |
| **~20:20** | فحص نهائي: 16 حاوية جميعها healthy، القرص 48% استخدام (102GB متاح) — **الحادثة مُغلَقة** |

---

## 3. Root Cause Analysis

### لماذا وصل `fetch-cache` إلى 111GB؟
صفحات الرياضة (`match/[id]`, `player/[id]`, `team/[id]`, `competition/[id]`) هي مسارات Next.js ديناميكية بالكامل **بدون `generateStaticParams`**، والتحقق الوحيد من المعرّف هو `Number.isInteger(id) && id > 0` — أي رقم صحيح موجب يُقبل ويُطلق طلبات فعلية لـ 365Scores. لا حدّ أعلى، لا قائمة IDs مسموحة، لا تحقق من وجود المباراة/اللاعب فعلياً قبل الطلب.

### لماذا وُجد 3.1 مليون ملف؟
كل زيارة لصفحة مباراة واحدة تُطلق **6 إلى 8 استدعاءات `fetch()` منفصلة** (`getGameDetail`, `getCompetitionMeta`, `getCompetitionMatchList`, `getStandings`, `getShotMap`, `getH2H`, وحسب التبويب النشط: `getGameStats`/`getPreGameStats`/`getMatchTrends`) — كل استدعاء بمفتاح كاش فريد (URL يتضمن `gameId`/`athleteId`/`competitionId`/`teamId`/`seasonKey`). بارامترات `COMMON` بكل استدعاء **ثابتة تماماً** (لا timestamp، لا قيمة لكل مستخدم) — التحقق المباشر من الكود نفى فرضية "تلوّث المفتاح ببارامترات متغيرة". العامل الوحيد للتضخم هو تنوّع المعرّفات نفسها، مضروباً في 6-8 مفاتيح لكل صفحة.

### لماذا لم يُحذف الكاش تلقائياً؟
لا يوجد `cacheHandler` مخصّص في `next.config.ts` — Next.js يستخدم معالج الكاش الافتراضي (نظام ملفات) الذي **لا يملك أي آلية Eviction أو حد أقصى للحجم**. حقل `revalidate` بكل استدعاء (`revalidate: 30` إلى `revalidate: 86400` حسب نوع البيانات) يتحكم فقط بمتى يُعتبر الملف "قديماً ويحتاج تحديث عند الطلب التالي" — لا يحذف الملف القديم أبداً، بل يستبدل محتواه فقط عند إعادة الطلب. أي مفتاح لم يُطلب مجدداً (مباراة قديمة، لاعب نادر) يبقى على القرص للأبد.

### لماذا أدى ذلك لتجاوز Restart Limit في Coolify؟
عند امتلاء القرص 100%، فشلت كل الكتابات الجديدة (health checks، لوجز، بناء صور) — حاويات `worker`/`worker-media` (تحتاج اتصال فوري بـ Redis عند الإقلاع) و`backend`/`frontend`/`scheduler`/`admin` فشلت بالإقلاع أو باجتياز health check. سياسة `restart: unless-stopped` في `docker-compose.yml` تعني إعادة محاولة لا نهائية من Docker نفسه، لكن Coolify يفرض حد أقصى منفصل (10 محاولات) قبل أن **يحذف الحاويات نهائياً** (وليس فقط إيقافها) — سلوك تصميمي لمنع حلقات إعادة تشغيل عبثية، لكنه أدى هنا لحذف كامل للـ stack بدل تركه متوقفاً قابلاً لإعادة التشغيل اليدوي البسيط.

---

## 4. Contributing Factors

| العامل | الأثر |
|---|---|
| **Next.js File Cache بلا حد** | المعالج الافتراضي (fs) ليس مصمَّماً لبيانات طرف ثالث عالية التنوّع — مناسب لمحتوى تحريري محدود العدد (مقالات)، لا لبيانات رياضية بملايين المعرّفات المحتملة |
| **مسارات ديناميكية مفتوحة** | `dynamicParams` غير مقيّد على 4 مسارات رياضة، بلا `generateStaticParams` ولا تحقق من وجود الكيان قبل الطلب |
| **8 fetches لكل صفحة مباراة** | يضاعف أثر أي تنوّع بالمعرّفات ×6-8 من حيث عدد ملفات الكاش والطلبات لـ365Scores |
| **غياب Access Logs بالكامل** | Traefik يعمل بلا `--accesslog` — يستحيل إثبات ما إذا كان مصدر التنوّع الهائل بالمعرّفات بوتات زحف (Googlebot/Bingbot/زاحف آخر) أو زيارات بشرية عادية أو مزيج، ما أطال التحقيق وسيتكرر في أي حادثة مستقبلية مشابهة |
| **غياب تنبيهات امتلاء القرص** | لا نظام مراقبة (Netdata/Prometheus/تنبيه بريدي) يكشف تجاوز 80%/90% استخدام القرص — الحادثة اكتُشفت فقط بعد التوقف الكامل، لا قبله |
| **مجلد التطبيق على السيرفر ليس مصدر الحقيقة** | `/data/coolify/applications/<uuid>/` يحوي فقط `.env` و`docker-compose.yaml` المولَّد — لا كود مصدر فعلي (Dockerfile) إلا مؤقتاً داخل حاوية بناء Coolify، ما استلزم استنساخ المستودع يدوياً لبناء صورة احتياطية |
| **الشبكة الخاصة بالتطبيق كانت محذوفة** | شبكة `se1ufsuugt5wdb0iw7b40vgv` (المطلوبة كـ`external: true`) اختفت ضمن تنظيف Coolify بعد حذف الحاويات — لولا إعادة إنشائها يدوياً، حتى إعادة نشر Coolify الطبيعية كانت ستفشل |

---

## 5. What Went Well

- **قاعدة البيانات وRedis وMeilisearch (النسخة القديمة) بقوا سليمين وشغّالين طوال الحادثة بلا انقطاع** — لم يدخلوا بحلقة إعادة تشغيل إطلاقاً، وامتلاء القرص لم يتلف بياناتهم.
- **نجاح النسخة الاحتياطية**: `mysqldump` مضغوط (482MB، قاعدتا `default` و`gasem`) بُثّ مباشرة عبر SSH لجهاز محلي (تجاوز نقص المساحة على السيرفر بالكامل)، وتم التحقق من سلامته (`gzip -t` + وجود علامة `-- Dump completed`) قبل أي إجراء حذف.
- **حذف الكاش لم يمسّ أي بيانات إنتاج**: اقتصر الحذف على `fetch-cache` فقط (لا `.next` كاملاً، لا volumes أخرى) — تحرّر 111GB بأمان تام خلال 5 دقائق.
- **نجاح إعادة النشر النهائية من Coolify نفسه** بعد إصلاح شبكة `se1ufsuugt5wdb0iw7b40vgv` المفقودة.
- **صفر فقدان بيانات** مؤكَّد: 117 جدول MySQL سليمة، `/data/uploads` (40GB وسائط) و`/data/storage-public` سليمين لم يُلمسا.
- **لا حاجة لأي تغيير معماري طارئ تحت ضغط** — تم الالتزام بفصل "استعادة أولاً، إعادة تصميم لاحقاً على بيئة تطوير منفصلة".

---

## 6. What Went Wrong

- **إضاعة وقت على محاولة Redeploy على تطبيق Coolify خاطئ** (site آخر تمّت رؤيته بالخطأ) — أدّى لتشخيص أولي خاطئ (الشك بوجود متغيّر `BUILD_API_BASE_URL` معطوب فعلياً بتطبيق alqla) استغرق وقتاً قبل أن يوضّح المستخدم أنه كان ينظر للسيرفر/التطبيق الخطأ.
- **افتراض خاطئ رئيسي**: بعد فشل محاولة Redeploy الأولى (بسبب مشكلة "chicken-and-egg" — البناء يتطلب backend حيّاً)، تم الافتراض أن الاستعادة تتطلب تدخلاً يدوياً كاملاً (استنساخ الكود، بناء صورة backend، تشغيل حاوية بإعدادات شبكة/Traefik مطابقة يدوياً) — بينما Coolify كان، بشكل مستقل، **قد نجح فعلاً بإعادة نشر التطبيق كاملاً** (على الأغلب من محاولة تالية للمستخدم بعد تصحيح التطبيق الصحيح). لم يُكتشف هذا إلا بعد إكمال محاولة يدوية معقدة (بناء صورة، حل مشاكل شبكات، صياغة Traefik labels يدوياً) كانت غير ضرورية بالكامل.
- **تعارض مباشر نتج عن الافتراض الخاطئ**: تشغيل حاوية backend يدوية بنفس اسم Traefik router الذي يستخدمه backend الحقيقي (Coolify-managed) سبّب خطأ "Router defined multiple times" و503 مؤقت — كان يمكن تجنّبه بفحص `docker ps -a` بشكل أشمل *قبل* بدء أي محاولة يدوية جديدة، لا فقط بعد فشل أول محاولة.
- **الوقت الإجمالي للاستعادة أطول من اللازم**: لو تم التحقق من حالة إعادة نشر Coolify (`docker ps -a --filter name=<app-uuid>`) بشكل دوري ومباشر بعد كل محاولة Redeploy من المستخدم، لكان اكتشاف نجاح Coolify الذاتي أسرع بكثير من ساعة كاملة تقريباً من العمل اليدوي المتوازي غير الضروري.

---

## 7. Corrective Actions (منفَّذة فوراً خلال الحادثة)

- [x] نسخة احتياطية كاملة ومتحقَّقة لقاعدتي البيانات (`default`, `gasem`) قبل أي إجراء حذف.
- [x] حذف محتوى `fetch-cache` فقط (3.1 مليون ملف، 111GB مُحرَّرة) — بدون لمس `.next` كاملاً أو أي volume آخر.
- [x] إعادة إنشاء شبكة Docker المفقودة (`se1ufsuugt5wdb0iw7b40vgv`) الضرورية لعمل Coolify نفسه.
- [x] التحقق من نجاح إعادة النشر الكاملة (7 حاويات: backend/frontend/worker/worker-media/scheduler/admin/meilisearch) — جميعها healthy.
- [x] التحقق من صحة المحتوى المنشور فعلياً (HTTP 200 على `api.alpha-cms.shop` و`alpha-cms.shop`، عنوان الصفحة يطابق الهوية الصحيحة).
- [x] إزالة كل مخلفات المحاولات اليدوية (حاوية `backend-manual-restore`، صورة `alphacms-backend:latest`، مجلد `/root/alqla-src`) بعد التأكد أنها غير مستخدَمة من أي حاوية حيّة.
- [x] فحص نهائي شامل: `docker ps`، `docker system df`، `df -h` — القرص 48% استخدام، كل الحاويات healthy.

---

## 8. Preventive Actions (مخطَّطة — تُنفَّذ على بيئة تطوير منفصلة قبل أي نشر إنتاجي)

هذه البنود موضوع **التذكرة الأولى** المفتوحة بشكل منفصل (أولوية عالية):

1. **إعادة تصميم معمارية 365Scores**: Laravel كوسيط وحيد بين Next.js و365Scores (لا اتصال مباشر من Next.js إطلاقاً على مستوى الكود، لا فقط الشبكة — واجهة داخلية موحّدة مثل `getMatch()` تخفي كل تفاصيل المزوّد).
2. **Redis بدل fetch-cache**: TTL متدرّج حسب نوع البيانات (15-30s للحيّ، 5-10min للإحصائيات، ساعة+ للاعبين) بدل ملف واحد بلا حدود.
3. **تحقق كسول من الـ IDs**: Redis → 365Scores → تخزين/404 (لا رفض فوري لمعرّف غير مخزَّن محلياً بعد).
4. **`MatchAggregator`** يجمّع من خدمات داخلية منفصلة (لا endpoint واحد بـ TTL موحَّد يفرض Cache Thrashing).
5. **Rate Limiting**: تفعيل `throttle:public.sport` على نمط `throttle:public.read` الموجود أصلاً وبشكل ناضج بباقي الـ API العام.
6. **`robots.txt`**: استثناء `/sport/player/*` و`/sport/team/*` من الزحف (البقاء على السماح لـ`/sport/match/*` ما لم يظهر سبب SEO واضح لمنعه لاحقاً).
7. **Cloudflare WAF**: قاعدة حظر/تحدٍّ عند تجاوز IP واحد لعدد كبير من المسارات الفريدة تحت `/sport/` خلال دقيقة (تنفَّذ من طرف صاحب حساب Cloudflare مباشرة).
8. **Monitoring وDisk Alerts**: تنبيهات عند القرص >80% و>90%، `frontend-cache` (أو بديله) >5GB، استهلاك ذاكرة Redis >80%.
9. **تفعيل Access Logs على Traefik**: ثغرة تشخيصية اكتُشفت أثناء هذه الحادثة نفسها — بدونها يستحيل إثبات مصدر أي ارتفاع غير طبيعي بالطلبات مستقبلاً (بوت أم بشر).
10. **اختبار ضغط قبل النشر**: تشغيل السكربت محلياً لساعات، فتح آلاف صفحات الرياضة، التأكد أن الكاش الجديد لا يتضخم بشكل غير طبيعي، لا طلبات مباشرة من Next.js لـ365Scores، الأداء جيد — قبل أي `git push`/Deploy.

---

## 9. Lessons Learned

- **الكاش الافتراضي لأي إطار عمل ليس "آمناً بحكم الافتراض"** — `next: { revalidate }` يبدو كأنه يضبط "عمر" البيانات، لكنه فعلياً لا يحذف شيئاً أبداً في المعالج الافتراضي المعتمد على نظام الملفات. أي بيانات طرف ثالث عالية التنوّع (لا محتوى تحريري محدود) يجب أن تُستبعد من هذا النمط من الكاش من اليوم الأول، لا بعد الحادثة.
- **مسار ديناميكي مفتوح (`[id]`) بلا `generateStaticParams` ولا تحقق من الوجود هو سطح هجوم/استنزاف، لا مجرد راحة برمجية** — ينطبق هذا على أي ميزة مستقبلية تتعامل مع مصدر بيانات خارجي بمعرّفات عالية التنوّع.
- **غياب Access Logs يكلّف وقت تحقيق حقيقياً** — تم اكتشاف هذه الثغرة التشخيصية *أثناء* محاولة تفسير سبب وصول العدد لـ3.1 مليون ملف، لا قبلها. يجب أن تكون جزءاً أساسياً من أي بنية إنتاجية من البداية.
- **تحقّق من حالة النظام الفعلية أولاً، قبل افتراض الحاجة لتدخل يدوي معقّد** — الدرس الأهم تشغيلياً من هذه الحادثة تحديداً: بعد أي فشل بخطوة استعادة (هنا: فشل Redeploy الأول)، الخطوة التالية الصحيحة هي **إعادة فحص الحالة الكاملة للنظام** (`docker ps -a` شامل، لا فلترة ضيقة) قبل البدء بأي مسار بديل معقّد — لا الانتقال المباشر لحل يدوي بديل بافتراض أن النظام لا يزال بنفس الحالة الفاشلة.
- **مصدر الحقيقة لإعدادات الإنتاج (شبكات، Traefik labels) يجب أن يكون موثَّقاً خارج Coolify نفسه** — الاعتماد الكامل على استخراج هذه الإعدادات من ملف مولَّد فقط وقت الحاجة أبطأ أي استعادة طارئة مستقبلية.

### تحديثات مقترحة على Runbook الاستعادة
1. عند أي حادثة توقف حاويات: **أول خطوة دائماً** = `docker ps -a --filter name=<app-uuid-or-name>` شامل (بلا فلترة اسم خدمة واحدة) للتحقق من الحالة الفعلية الكاملة — كرّرها بعد أي محاولة Redeploy من لوحة Coolify، قبل البدء بأي مسار بديل.
2. عند الشك بحالة تطبيق على Coolify: **تأكيد اسم/UUID التطبيق ورابطه بشكل صريح** في كل مرة قبل الضغط على Redeploy — خاصة بوجود أكثر من موقع/تطبيق على نفس لوحة Coolify.
3. توثيق (خارج Coolify) قائمة بالشبكات الخارجية (`external: true`) والـ Traefik labels الأساسية لكل تطبيق — نسخة احتياطية يدوية بسيطة تُسرّع أي استعادة طارئة مستقبلية دون الحاجة لاستخراجها من ملف مولَّد وقت الأزمة.
4. أي عملية حذف بيانات إنتاج (حتى كاش "قابل لإعادة التوليد") تسبقها نسخة احتياطية مُتحقَّق منها + تأكيد صريح من صاحب القرار — طُبِّق هنا بنجاح ويجب أن يبقى معياراً ثابتاً.

---

## Rejected Hypotheses (فرضيات تم التحقيق فيها واستبعادها)

توثيق هذه الفرضيات مهم لتجنّب إعادة التحقيق بنفس المسارات في أي حادثة مشابهة مستقبلاً.

| الفرضية | كيف استُبعدت | النتيجة |
|---|---|---|
| **تلف/فقدان بيانات قاعدة البيانات** | فحص مباشر: `docker ps` أظهر حاوية MySQL شغّالة وصحية طوال الحادثة (لم تدخل بحلقة restart إطلاقاً). لاحقاً `php artisan db:show` أكّد 117 جدول سليمة، 2.76GB. نسخة احتياطية كاملة أُخذت ووُثِّق نجاحها كإجراء احترازي إضافي رغم استبعاد الفرضية | **مستبعدة تماماً** — المشكلة تشغيلية (Storage/Cache) بحتة، لا علاقة لها بالبيانات |
| **مشكلة Traefik أو شهادات SSL كسبب أساسي للتوقف** | فُحصت لوجات Traefik (`docker logs coolify-proxy`) — الأخطاء الموجودة (شهادات ACME فاشلة لدومينات قديمة/خاطئة: `delpoy.alpha-cms.shop`, `coolify.alpha-cms.shop`) غير متعلقة بدومين alqla الأساسي، وتعود لتواريخ سابقة للحادثة (2026-07-24 وما بعدها كضجيج مستمر) | **مستبعدة كسبب أساسي** — Traefik نفسه كان يعمل بشكل صحيح طوال الوقت (Up لأسابيع بلا انقطاع)؛ 503 الوحيد الذي ظهر لاحقاً كان تعارض تسمية بسبب محاولة استعادة يدوية زائدة، لا مشكلة أصلية بـTraefik |
| **مشكلة بنفس نمط الحادثة السابقة الموثَّقة (`INCIDENT_ANALYSIS_2026-07-09.md`) — env_file/volumes مفقودة** | فُحص `docker-compose.yml` مباشرة: كل إصلاحات الحادثة السابقة (env_file على backend/worker/worker-media/scheduler، volumes الموحَّدة عبر anchor) **موجودة ومنشورة فعلياً** على الإنتاج. تأكيد إضافي: `grep env_file` على الملف المنشور على السيرفر أظهر وجودها بكل الخدمات السبع | **مستبعدة** — هذا حادث جديد ومختلف جذرياً عن حادثة يوليو، رغم تشابه العرض النهائي (restart loop) |
| **تكرار حادث Docker logs غير محدودة الحجم كسبب لامتلاء القرص** | فُحص `/etc/docker/daemon.json` مباشرة: `log-driver: json-file` مع `max-size: 10m` و`max-file: 3` مُفعَّل بشكل صحيح. تأكيد إضافي: فحص أحجام ملفات اللوج الفعلية لكل حاوية — كلها بحدود ميجابايتات قليلة، لا غيغابايتات | **مستبعدة تماماً** — إعدادات دوران اللوجز سليمة ولا تحتاج أي تعديل |
| **إعادة تشغيل السيرفر نفسه (host reboot) كسبب أو كإجراء استعادة مطلوب** | `uptime` عند بداية التحقيق أظهر السيرفر يعمل منذ 13 يوماً و23 ساعة بلا انقطاع — لا علاقة لإعادة تشغيل السيرفر بالحادثة، والاستعادة تمّت بالكامل عبر إدارة الحاويات فقط دون أي حاجة لإعادة تشغيل الجهاز | **مستبعدة** — لم تكن مطروحة فعلياً كخطوة استعادة، ذُكرت هنا للتوثيق الشامل فقط |
| **اختراق أمني (Compromise) كسبب لسلوك غير طبيعي** | فُحص: سجل تسجيلات الدخول الأخيرة (`last -a`) — كلها من نفس عنوان IP داخلي (`10.0.1.5`) لمستخدم root، لا محاولات دخول مشبوهة. `fail2ban` نشط على sshd. الطلبات المشبوهة الوحيدة الظاهرة بلوجات Traefik (محاولات لمسارات مثل `cloud.php`, `xmrlpc.php` على دومين `psono.fr` غير المرتبط بالموقع) هي ضجيج مسح تلقائي عشوائي من الإنترنت (شائع لأي IP عام)، لا دليل استهداف مباشر أو اختراق فعلي | **مستبعدة** — لا دليل على اختراق؛ السبب تقني بحت بالكامل (تصميم كاش) |
| **متغيّر `BUILD_API_BASE_URL` معطوب فعلياً بإعدادات تطبيق alqla على Coolify (يشير لدومين موقع آخر — `harer.store`)** | فُحص أول الأمر كفرضية جدّية بعد ظهورها بلوج بناء فاشل. استُبعدت لاحقاً بتوضيح مباشر من صاحب القرار: كان تبويب Coolify مفتوحاً بالخطأ على تطبيق/سيرفر مختلف تماماً وقت تلك المحاولة تحديداً، لا خللاً حقيقياً بإعدادات alqla | **مستبعدة** — خطأ بشري بتحديد التطبيق الصحيح على اللوحة، لا خلل إعدادات فعلي بتطبيق alqla |

---

## ملحق: أرقام مرجعية سريعة

- حجم `fetch-cache` قبل التنظيف: **111GB** (3,107,756 ملف)
- مساحة محرَّرة: 111GB (القرص من 100% → 43% فوراً بعد الحذف، 48% بعد اكتمال إعادة النشر)
- حجم النسخة الاحتياطية لقاعدتي البيانات: 482MB مضغوطة (`default` + `gasem`، 2.76GB + ~2.8GB غير مضغوطتين)
- عدد الحاويات المستعادة: 7 (backend, frontend, worker, worker-media, scheduler, admin, meilisearch)
- عدد محاولات إعادة التشغيل قبل حذف Coolify للحاويات: 65 (الحد الأقصى المُعدّ: 10)
