# تحقيق معمّق: مصدر الـ 5.5GB الجديدة بـ `.next/cache`

**لا تعديل كود، لا Commit.** فحص مباشر لمحتوى الكاش الفعلي على السيرفر (`docker exec` داخل حاوية frontend المنشورة، عمرها ~3 ساعات وقت الفحص) + اختبارات تجريبية آمنة (GET فقط) على الموقع الحيّ للتحقق بدلاً من التخمين.

---

## السؤال الأهم: من كتب الـ 5.5GB؟ — جواب قاطع بالأرقام

```
136,058 ملف بـ fetch-cache (وقت الفحص، استمر بالنمو أثناء التحقيق نفسه إلى 139,805+)
متوسط حجم الملف: 82.5KB — لا ملفات عملاقة مفردة (أكبر ملف 1.9MB فقط) — النمو من الكمّ لا من حجم الملف
```

**التوزيع حسب الدومين المصدر** (استخرجته من حقل `url` المخزَّن داخل كل ملف كاش نفسه — بيانات مباشرة، لا تخمين):

| الدومين | عدد الملفات | النسبة |
|---|---|---|
| `webws.365scores.com` | **135,696** | **98.7%** |
| `api.alpha-cms.shop` (Backend الخاص بنا) | 1,713 | 1.25% |
| `api.openweathermap.org` | 24 | <0.1% |
| `www.ase.com.jo` | 7 | <0.1% |
| `api.royanews.tv` | 1 | <0.1% |

**الجواب المباشر على سؤالك**: **كلها تقريباً (98.7%) من الرياضة.** النمو **ليس** موزَّعاً بشكل معماري عام عبر المشروع كما كنا نخشى — هو مُركَّز بشكل ساحق بمصدر واحد. **يجب أن تبدأ المعالجة من الرياضة أولاً**، تماماً كما اقترحت كمعيار قرار. الـ 1,713 ملف من `api.alpha-cms.shop` (المقالات/التفاعل) موجودة وموثّقة أدناه (قسم 4) لكنها ثانوية حجماً الآن — تستاهل نفس المعالجة المعمارية لاحقاً، لا أولوية فورية.

---

## 1. تحليل محتوى `.next/cache` حسب النوع

| المجلد | الحجم | عدد الملفات |
|---|---|---|
| `fetch-cache/` | 5.5GB | 136,058 (وينمو) |
| `webpack/` | 440.1MB | 6 (ثابت — كاش بناء، لا ينمو مع الزوار) |
| `swc/` | 12.0KB | 0 |
| `eslint/` | 184.0KB | 1 |
| `images/`, `turbopack/` | — | **غير موجودين إطلاقاً** — هذا المشروع لا يستخدم Image Optimization Cache ولا Turbopack بالإنتاج |

**الخلاصة**: `fetch-cache` وحده هو 99.9% من حجم `.next/cache` بالكامل. لا مصدر نمو ثانٍ داخل هذا المجلد.

---

## 2. تحليل `fetch-cache` — تفصيل مسارات 365Scores (الأهم)

| مسار 365Scores | عدد الملفات | % من الإجمالي |
|---|---|---|
| `/web/game/` (تفاصيل مباراة) | 33,037 | 24.3% |
| `/web/athletes/career` (مسيرة لاعب) | **25,764** | **18.9%** |
| `/web/athletes/` (فرق اللاعب) | 21,432 | 15.8% |
| `/web/stats/` | 11,168 | 8.2% |
| `/web/athletes/games/` | 8,311 | 6.1% |
| `/web/trends/` | 5,944 | 4.4% |
| `/web/squads/` | 4,117 | 3.0% |
| `/web/athletes/trophies/stats` | 3,638 | 2.7% |
| `/web/game/stats/` | 3,023 | 2.2% |
| باقي المسارات (results, competitions, fixtures, standings...) | ~7,635 | 5.6% |

**مجموع مسارات "athletes" (اللاعبين) لوحدها = 59,145 ملف (43.5% من كامل fetch-cache)** — أكبر من كل مسارات المباريات مجتمعة. هذا اكتشاف جديد لم يظهر بالتحليل الأول للحادثة (يوم 2026-08-07 الصباحي)، الذي ركّز على صفحة المباراة كمصدر رئيسي.

---

## 3. ربط مباشر بالكود — لماذا "athletes" تحديداً هي الأكبر (لا تخمين، سطر بسطر)

### السبب الجذري الجديد: `getPlayerCareerData` تولّد **10 ملفات كاش من زيارة واحدة**

**[frontend/src/lib/sport/player.ts:342-353](frontend/src/lib/sport/player.ts#L342-L353)**:
```ts
// career يلزمه seasonKey ويعطي بطولات ذلك الموسم فقط ⇒ نمسح ١٠ مواسم بالتوازي...
export const getPlayerCareerData = cache(async (athleteId: number): Promise<PlayerCareerData> => {
  const y = new Date().getFullYear();
  const seasons = Array.from({ length: 10 }, (_, i) => y - i);
  const all = await Promise.all(seasons.map((s) => fetchCareer(athleteId, s).catch(() => null)));
  ...
});
```
الدالة `fetchCareer` (**[player.ts:316-320](frontend/src/lib/sport/player.ts#L316-L320)**) نفسها:
```ts
async function fetchCareer(athleteId: number, seasonKey: number): Promise<CareerFetch | null> {
  const res = await fetch(`${BASE}/athletes/career?${COMMON}&athleteId=${athleteId}&seasonKey=${seasonKey}`, {
    next: { revalidate: 3600, tags: ['sport-stats'] },
  });
  ...
}
```
**كل موسم = مفتاح كاش منفصل** (`athleteId` + `seasonKey` معاً بالـ URL). زيارة **واحدة** لصفحة لاعب واحد ⇒ **10 استدعاءات متوازية، 10 ملفات كاش جديدة، فوراً**، بلا أي علاقة بعدد الزوار الفعليين لتلك الصفحة تحديداً — عامل تضخيم ×10 مضروب بعدد اللاعبين المُزارين/المفهرَسين.

### مضاعِف ثانٍ: `getPlayerTrophies` تولّد حتى **20 ملف إضافي** لنفس الزيارة

**[player.ts:398-431](frontend/src/lib/sport/player.ts#L398-L431)**:
```ts
export const getPlayerTrophies = cache(async (athleteId: number, competitions: {id:number,name:string}[]) => {
  const list = competitions.slice(0, 20);
  const results = await Promise.all(
    list.map(async ({ id: cid }) => {
      const res = await fetch(`${BASE}/athletes/trophies/stats?${COMMON}&athleteId=${athleteId}&competitionId=${cid}`, {
        next: { revalidate: 3600, tags: ['sport-stats'] },
      });
      ...
```
مفتاح الكاش هنا = `athleteId` + `competitionId` — **حتى 20 طلب متوازٍ لكل بطولة شارك بها اللاعب عبر مسيرته**.

**النتيجة الكاملة**: زيارة واحدة فقط لـ `/sport/player/[id]` (route: **[frontend/src/app/(site)/sport/player/[id]/page.tsx:61-67](frontend/src/app/(site)/sport/player/[id]/page.tsx#L61-L67)**) يمكن أن تولّد **حتى 30+ ملف كاش دفعة واحدة** (10 من career + حتى 20 من trophies + عدد إضافي من `getPlayer`/`getPlayerTeams`/`getPlayerRecentGames`) — أضعاف مضاعف حادثة صفحة المباراة الأصلية (6-8 ملفات). **هذا هو التفسير المباشر، المؤكَّد بالكود، لماذا athletes = 43.5% من الحجم رغم افتراضنا الأولي أن المباريات هي الأكثر خطورة.**

---

## 4. المقالات (`article/[id]`) — تقرير مستقل كما طلبت

جميع النقاط التالية موثَّقة مباشرة من **[frontend/src/lib/articles.ts:283-298](frontend/src/lib/articles.ts#L283-L298)**:

```ts
export const getArticle = cache(async (idslug: string, locale = 'ar'): Promise<ArticleDetail | null> => {
  if (!env.apiBaseUrl) return null;
  const bare = idslug.replace(/^\d+-/, '');
  try {
    const res = await fetch(`${env.apiBaseUrl}/api/v1/${enc(locale)}/articles/${enc(idslug)}`, {
      next: { revalidate: 36000, tags: ['articles', `article:${bare}`] },
    });
    if (!res.ok) return null;
    ...
```

| السؤال | الجواب | كيف تحقّقت |
|---|---|---|
| هل يقبل أي ID؟ | **نعم** — `idslug` يُمرَّر خام بلا أي فحص صيغة/رقم قبل الـ fetch | قراءة مباشرة للكود، لا `Number.isInteger` ولا regex قبل السطر 287 |
| ماذا يحدث عند ID غير موجود؟ | Backend يرجّع 404 → `if (!res.ok) return null` → الصفحة تستدعي `notFound()` (Next 404 قياسي) | — |
| هل يرسل Request للـ API؟ | **نعم، دائماً** — الطلب يُرسَل فعلياً لـ365 (Laravel) بغض النظر عن صحة الـ ID | — |
| **هل يخزّن 404؟** | **لا — اختبرت تجريبياً على الموقع الحيّ، لم أخمّن** | طلبت `https://alpha-cms.shop/article/999999999-nonexistent-audit-test` (HTTP 404 فعلي)، ثم بحثت بكامل `fetch-cache` (139,760+ ملف) عن الرقم `999999999` — **صفر نتائج، `grep` رجّعت exit code 1 (لا تطابق)**. الاستنتاج: نسخة Next.js هذه (v15.5.19، تأكَّدت من `ps aux` داخل الحاوية) **لا تخزّن استجابات غير-200 بالـ fetch cache**، خلافاً لافتراضي النظري الأول قبل الاختبار |
| هل يولّد Cache Key جديد لكل رقم؟ | **نعم، لكن فقط للـ IDs الموجودة فعلياً** (بما أن غير الموجودة لا تُخزَّن حسب الاختبار أعلاه) | استنتاج مباشر من نتيجة الاختبار السابق |

**الخلاصة**: مخاطر `article/[id]` حقيقية (لا `generateStaticParams`، TTL طويل 36000s=10 ساعات) لكنها **أقل حدّة مما افترضنا أولاً** — لأن محاولات تخمين IDs عشوائية غير موجودة **لا تكلّف مساحة قرص** (فقط تكلّف طلب شبكة فاشل لـ Laravel، عبء أداء لا تخزين). الخطر الحقيقي مقتصر على الحجم الفعلي لعدد المقالات الحقيقية المنشورة × الزيارات إليها — أقل انفجاراً بكثير من مساحة IDs الرياضة المفتوحة على 365Scores.

---

## 5. البحث (`?q=`) — هل كل استعلام كاش منفصل؟

**الكود ([frontend/src/lib/feed.ts:397-415](frontend/src/lib/feed.ts#L397-L415)، عبر `searchArticles` في `search/page.tsx`)**: `filter[q]` يدخل مباشرة بالـ URL المُخزَّن، مع `next: { revalidate: 60, tags: ['articles','search'] }`. **بالتصميم، كل نص مختلف = URL مختلف = مفتاح كاش مختلف.** هذا مؤكَّد من الكود مباشرة، بلا حاجة لتخمين آلية العمل.

**اختبار تجريبي** (3 طلبات فعلية لاستعلامات عشوائية `xaudittest1/2/3`): وجدت ملف كاش واحد على الأقل يحتوي فعلياً نص `xaudittest` — **يثبت تجريبياً أن نص بحث عشوائي غير موجود بالنظام يُنتج ملف كاش جديداً وفعلياً**. (فحص تجريبي إضافي للتأكد من الملفات الثلاثة تحديداً بدل واحد فقط كان لا يزال قيد التنفيذ على السيرفر الحيّ وقت كتابة هذا التقرير بسبب ضغط عمليات مسح متعددة متزامنة — الدليل الكودي + التأكيد التجريبي الجزئي كافيان لجواب قاطع).

**الجواب**: **نعم — هذا فعلاً ثقب أخطر من الرياضة من ناحية طبيعته**، تماماً كما توقّعت. الفرق الجوهري عن الرياضة: مساحة مفاتيح الرياضة محدودة بعدد كيانات 365Scores الحقيقية (رياضيين/مباريات فعلية، ملايين لكن محدودة العدد)، بينما مساحة مفاتيح البحث الحر **غير محدودة رياضياً** (أي سلسلة نصية ممكنة) — TTL قصير (60s) يخفف الأثر الآني لكن لا يمنع الانفجار عند زحف/هجوم منهجي بسلاسل عشوائية متغيرة باستمرار.

---

## 6. Pagination (`?page=999999`)

**الكود**: `Math.max(1, Number(sp.page) || 1)` بلا أي حد أعلى على الأقل بثلاثة مواقع (`category/[id]/[name]`, `writer/[id]`, `search`) — مؤكَّد من تدقيق الكود الأول. **لا يوجد فحص وجود صفحة فعلية قبل تمرير الرقم للـ backend.**

بالنسبة لـ"هل ينشئ كاش دائم؟" — بناءً على نتيجة اختبار 404 بالمقالات (القسم 4)، إذا كان الـ backend يرجّع صفحة فارغة/غير-200 لرقم صفحة يتجاوز الفعلي، فمن المرجّح **جداً** (بنفس آلية الاختبار المؤكَّدة أعلاه) أنها **لا تُخزَّن أيضاً إذا كانت الاستجابة غير-200**. لكن إذا كان الـ backend يرجّع **200 بمصفوفة بيانات فارغة** (سلوك شائع بالـ APIs المرقّمة بدل 404) — عندها **تُخزَّن فعلاً** كملف كاش صالح دائم (حتى انتهاء الـ TTL، 60-36000s حسب المسار). **هذا يعتمد على سلوك الـ Laravel API تحديداً لرقم صفحة خارج النطاق، ولم أتحقق منه تجريبياً بعد** (الفحص التجريبي المباشر لـ`?page=999999` كان قيد التنفيذ وقت كتابة هذا التقرير، لم يكتمل — **هذه نقطة الوحيدة بالتقرير التي لم أحصل فيها على تأكيد تجريبي كامل، أوضحها صراحة بدل الادّعاء بمعرفتها**).

---

## 7. الملفات المؤقتة `/tmp/restore_*` — تحقيق كما طلبت، بلا حذف

| السؤال | الجواب |
|---|---|
| من أنشأها؟ | **آلية النسخ الاحتياطي/الاستعادة الخاصة بـCoolify نفسه** — أكَّدته بقراءة محتوى السكربتات المرافقة مباشرة:<br>`mysql -u $MYSQL_USER -p$MYSQL_PASSWORD $MYSQL_DATABASE < '/tmp/restore_z88fmsyjepgj6bnxljbn6phq'`<br>`mariadb -u $MARIADB_USER ... < '/tmp/restore_rcd3qgv7ryznwizhj3a46b6n'`<br>أسماء متغيرات البيئة (`$MYSQL_USER`, `$MARIADB_USER`) هي نمط Coolify القياسي لتوليد سكربتات استعادة قواعد البيانات تلقائياً |
| هل مرتبطة بآخر Restore؟ | نعم — هذا بالضبط ما هي: ملفات dump جاهزة لعملية استعادة (اثنان بصيغة MySQL لقاعدتين مختلفتين، وواحد بصيغة MariaDB — ما يعني هذه العمليات شملت **قواعد بيانات تخص مشاريع أخرى على نفس السيرفر**، ليس alqla فقط — الأسماء `rcd3qgv7ryznwizhj3a46b6n` و`skbi559aje6qo5iwpoqzrtco` لا تطابق حاوية MySQL الخاصة بـ alqla) |
| هل ما زال Coolify يحتفظ بمراجع لها؟ | **غير مؤكَّد بشكل قاطع** — لا يوجد جدول `backups` بقاعدة بيانات Coolify الداخلية (Postgres) بهذا الاسم (قد يكون المخطط مختلفاً بهذا الإصدار)، لم أستطع تأكيد وجود/غياب مرجع من هذه الزاوية |
| هل مفتوحة من Process شغّال حالياً (`lsof`)? | **لا — تأكَّدت مباشرة، `lsof /tmp/restore_*` رجّع فارغاً تماماً، لا process يستخدمها الآن** |

**تاريخ الإنشاء**: 2026-07-25 (بين الساعة 02:21 و12:27) — **13 يوماً بالضبط**، بلا أي تغيير منذئذ.

**تقييمي (بدون حذف، القرار لك)**: الأدلة مجتمعة (لا process مفتوح، عمرها 13 يوم، طبيعتها "ملف dump لعملية استعادة مكتملة" لا "نسخة احتياطية جارية") تشير بقوة أنها **مخلّفات متروكة من عملية استعادة/اختبار استعادة اكتملت ولم يُنظَّف بعدها**. المخاطرة الوحيدة المتبقية غير المؤكَّدة 100%: عدم قدرتي على تأكيد أو نفي مرجع داخلي بقاعدة بيانات Coolify بشكل قاطع.

---

## ملخص الأولوية المُحدَّث (بناءً على هذا التحقيق)

1. **ابدأ من الرياضة فوراً** — 98.7% من المشكلة الحالية، مؤكَّد بالأرقام لا التخمين.
2. **داخل الرياضة، الأولوية القصوى هي `getPlayerCareerData`/`getPlayerTrophies`** (43.5% من الحجم، مضاعِف ×10-30 لكل زيارة لاعب) — أعلى أولوية من صفحة المباراة نفسها.
3. **البحث الحر (`?q=`) يستاهل نفس أولوية الرياضة**، رغم كونه 0% من الحجم الحالي — لأن طبيعته (مساحة مفاتيح لا نهائية رياضياً) أخطر بنيوياً، وقد لا يظهر أثره الكامل إلا عند تعرّضه لزحف/استغلال فعلي.
4. المقالات (`article/[id]`) أقل حدّة مما افترضنا أولاً بفضل اكتشاف "لا يُخزَّن 404" — أولوية متوسطة، ليست حرجة فورية.
5. `/tmp/restore_*` (8.4GB) — قرار منفصل بانتظارك، الأدلة تدعم الحذف لكن لم أنفّذه.
